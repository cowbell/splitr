<div class="container">
  <div class="panel">
    <div class="panel-heading"><strong><%= @budget.name %></strong> transactions</div>
    <table class="table table-hover .table-condensed">
      <tr>
        <th>Amount</th>
        <th>Description</th>
        <th>Participants</th>
      </tr>
      <% @budget.transactions.each do |transaction| %>
        <tr>
          <td><%= transaction.amount %></td>
          <td><%= transaction.description %></td>
          <td>...</td>
        </tr>
      <% end %>
      <% if can? :manage, @budget %>
        <tr>
          <td colspan="3"><%= link_to "Add", new_budget_transaction_path(@budget), class: "btn btn-primary btn-small" %></td>
        </tr>
      <% end %>
    </table>
  </div>

  <div class="panel">
    <div class="panel-heading"><strong><%= @budget.name %></strong> members</div>
    <table class="table table-hover table-condensed">
      <tr>
        <th>Name</th>
      </tr>
      <% @budget.members.each do |member| %>
        <tr>
          <td><%= member.name %></td>
        </tr>
      <% end %>
      <% if can? :manage, @budget %>
        <tr>
          <td><%= link_to "Add", new_budget_member_path(@budget), class: "btn btn-primary btn-small" %></td>
        </tr>
      <% end %>
    </table>
  </div>
</div>
